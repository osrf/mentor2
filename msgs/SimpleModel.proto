package Simple_msgs.msgs;

import "Variant.proto";

message SimpleModel
{
  required string name      = 1;
  optional string schematic_type = 2;
  repeated string port = 3;
  repeated string key = 4;
  repeated Variant value = 5;
}
